{
  "name": "Rubric",
  "type": "object",
  "properties": {
    "prompt_id": {
      "type": "string",
      "description": "FK to Prompt"
    },
    "passing_threshold": {
      "type": "number",
      "default": 70,
      "description": "Score threshold for passing (0-100)"
    }
  },
  "required": [
    "prompt_id"
  ]
}